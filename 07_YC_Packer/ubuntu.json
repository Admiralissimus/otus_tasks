{
    "builders": [
        {
            "type": "yandex",
            "folder_id": "b1g6mmcu4pmqf10ge9g2",
            "source_image_id": "fd80bm0rh4rkepi5ksdi",
            "image_name": "my_ubuntu_2204-{{timestamp}}",
            "image_description": "My ubuntu image created by packer",
            "use_ipv4_nat": true,
            "ssh_username": "admiral"
        }
    ],
    "provisioners": [
        {
            "type": "shell",
            "inline": [
                "echo 'updating APT'",
                "sudo apt-get update -y",
                "sudo apt-get install -y nginx",
                "sudo su -",
                "sudo systemctl enable nginx.service",
                "curl localhost"
            ]
        }
    ]
}